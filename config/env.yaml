latency_bars: 1
leverage_max: 1.0
window_bars: 96
deadband: 0.015
min_step: 0.01
# Optional: cap per-bar change in position (rate limiter). Lower to reduce churning.
max_step: 0.10
# Optional: position rounding grid (e.g., 0.1 -> multiples of 10%). 0 disables.
grid_step: 0.0
reward:
  kappa_cost: 10.0  # Increased from 1.0 to 10.0 to discourage overtrading
  kappa_turnover: 0.01  # Direct penalty on turnover (position change)
  lambda_risk: 0.001
  risk_metric: "dd_velocity"
costs:
  slippage_bps: 5
  commission_bps: 10
